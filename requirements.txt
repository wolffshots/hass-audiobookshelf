homeassistant==2025.1.4
ruff==0.16.0
pytest==9.1.1
aioaudiobookshelf==0.1.24
mypy==2.3.0
# aiodns declares pycares>=4.0.0, but pycares 5.x changed Channel.getaddrinfo()
# and breaks every DNS lookup in Home Assistant. Pin until aiodns supports it.
pycares<6
